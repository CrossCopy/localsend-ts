{
  "name": "localsend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build dev.ts --target=node --outdir=dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "valibot": "^1.0.0-rc.0"
  }
}
